- name: Deploy to Cloud Run
  id: deploy
  uses: google-github-actions/deploy-cloudrun@main
  with:
    service: chalupa-forwarder 
    image: gcr.io/cloudrun/hello
    credentials: ${{ secrets.GCP_SA_KEY }}

- name: Use Output
  run: curl "${{ steps.deploy.outputs.url }}"
